"""Placeholder so the lane has something to run before the module exists.

Replace with real tests as the module lands. The property tests each plan commits to
(graph invariants, monotonicity of u in evidence, escalation monotonic in uncertainty,
zero missed violations) belong here.
"""


def test_scaffold() -> None:
    assert True
