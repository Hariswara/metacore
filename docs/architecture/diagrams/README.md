# Diagrams

The architecture diagram must **visually distinguish** the three component classes, using the same
convention as the proposal:

- blue — rule-based / deterministic (the operational norm)
- orange — machine learning (introduced only where no adequate rule exists)
- green — the deterministic physics firewall

Any diagramming tool is acceptable. Export both an editable source and a rendered PNG/SVG here.
