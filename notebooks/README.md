# Notebooks

Exploratory scratch space, one folder per learned module. Notebooks **explore**; they never become
production code. Anything worth keeping graduates into the module's `src/` with tests.

Strip outputs before committing — `nbstripout` is the easiest way to keep diffs readable and the
repository small.
